# Normalise line endings so the repo stays LF regardless of the machine
* text=auto eol=lf

# Binary assets must never be line-ending converted
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
